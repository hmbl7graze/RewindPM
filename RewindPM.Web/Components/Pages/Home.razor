@page "/"
@inject NavigationManager Navigation

<PageTitle>RewindPM</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // ホームページからプロジェクト一覧にリダイレクト
        Navigation.NavigateTo("/projects", replace: true);
    }
}
